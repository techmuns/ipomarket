# Phase 5B.X — OnEMI Master/Detail Linkage Plan (planning only)

> **Mode**: planning. No code edits. No snapshot mutations. No linker implementation. **This document is the Gate 1 deliverable — it does not authorise Gate 2 (implementation).** Gate 2 requires separate explicit operator approval of the §8 implementation prompt below.
>
> **Date**: 2026-05-22
>
> **Predecessors**: `phase-5B1-production-promotion-plan.md` (Phase 5B.1 Gate 1), `phase-5B1-status.md` (Phase 5B.1 Gate 2 acceptance at commit `26b33c2`), `phase-5B-financial-normalization-plan.md`, `phase-5B-status.md`, `phase-5A.4-curated-official-pdf-seed-plan.md`, master plan §BB.
>
> **Trigger**: Phase 5B.1 closed accepted. OnEMI's financial periods landed in `ipo-financials.json` and a minimal RHP-linkage row landed in `ipo-documents.json`, but the §AA.10.1 UI-visibility caveat was honoured: `src/data/snapshots/ipo-master.json` was NOT touched. Consequence: OnEMI's financial + documents data is present in production snapshots, but the dashboard cannot surface it because 10 of the 10 routes / components consuming the master snapshot iterate `ipos[]` (the master array), not the financials / documents `by_ipo` keys.
>
> **Scope discipline (binding)**:
> - **OnEMI only** in this implementation; one new row to `ipo-master.json` only.
> - **DO NOT** mutate `ipo-financials.json`, `ipo-documents.json`, `ipo-narrative.json`, `ipo-source-audit.json`, or any other production snapshot.
> - **DO NOT** mutate `timelines[]` or `source_meta` blocks in `ipo-master.json`.
> - **DO NOT** mutate the 10 existing entries in `ipo-master.json`'s `ipos[]` array.
> - No UI changes UNLESS a Gate 2 render-time crash forces a tiny null-tolerance shim AND the operator separately approves it BEFORE landing.
> - No `.github/workflows/*` changes. No `scripts/ingest/*` changes. No `src/types/source.ts` changes. No `src/types/ipo.ts` schema changes (additive-optional only if strictly necessary).
> - No DB / Workers / cron / GMP / LLM inference / Chittorgarh fields / aggregator fields / PDF binaries / full-text dumps.
> - Stay on `main`.
>
> **Two-gate execution**: Gate 1 = this planning doc only (no code). Gate 2 = the implementation pass described in §8, requires separate explicit operator approval.

---

## 1. Objective

Add ONE minimal `Ipo` row for `onemi-technology-solutions` to `src/data/snapshots/ipo-master.json` so:

- The IPO is discoverable via the `/ipo/onemi-technology-solutions` slug route (existing `IpoDetail` page).
- The IPO surfaces in cross-route screens (Open & Upcoming / Screener / Pipeline / etc.) according to its `status` value.
- The Phase 5B.1 financial + documents rows finally have a parent master record, completing the dashboard's expected join.

Keep this **OnEMI-only and snapshot-only**. UI code changes are NOT in scope unless a Gate 2 render-time crash forces a separately-approved tiny null-tolerance shim.

---

## 2. Production target

**Primary target**: `src/data/snapshots/ipo-master.json` — ADD one new entry to the `ipos[]` array; do NOT mutate any of the 10 existing entries; do NOT touch `timelines[]` or `source_meta`.

### 2.1 Secondary snapshots — confirm whether they need touching

| Snapshot | Touch? | Rationale |
|---|---|---|
| `src/data/snapshots/ipo-financials.json` | **NO** | OnEMI already present from Phase 5B.1. |
| `src/data/snapshots/ipo-documents.json` | **NO** | OnEMI already present from Phase 5B.1. |
| `src/data/snapshots/ipo-narrative.json` | **NO** | OnEMI not present; UI render path for narrative typically null-tolerant (existing manual rows demonstrate this). Adding a narrative row would require operator-curated copy (strengths/risks/objectives) — out of Phase 5B.X scope. |
| `src/data/snapshots/ipo-source-audit.json` | **NO** | Auto-generated by `scripts/ingest/source-audit.ts` from probe results; not relevant to a manual master-row addition. Out of scope per §6. |
| `src/data/snapshots/ipo-listing-performance.json` | **NO** (presumed) | Pre-listing IPO has no listing-performance data; the UI must already null-tolerate this for any `status: 'upcoming'` row. To be confirmed at Gate 2 verification time. |
| `src/data/snapshots/ipo-subscriptions.json` | **NO** | Same as listing-performance — no subscription data for a not-yet-open IPO. |

### 2.2 Strong preference

Only `ipo-master.json` is mutated. If Gate 2 verification discovers a render-time crash from a missing secondary row, the implementation pass HALTS and reports back rather than silently widening scope.

---

## 3. Minimal OnEMI master fields

The `Ipo` interface (defined in `src/types/ipo.ts`) requires specific shape. Required (non-null) fields: `id`, `slug`, `name`, `segment`, `status`, `state`, `listing_exchange` (array, may be empty). All other fields admit `null`.

### 3.1 Row to add

```jsonc
{
  "id": "onemi-technology-solutions",
  "slug": "onemi-technology-solutions",
  "name": "OnEMI Technology Solutions",
  "segment": "mainboard",
  "status": "upcoming",
  "sector": null,
  "open_date": null,
  "close_date": null,
  "listing_date": null,
  "price_band": null,
  "lot_size": null,
  "issue_size_cr": null,
  "fresh_cr": null,
  "ofs_cr": null,
  "face_value": null,
  "listing_exchange": ["BSE"],
  "reservation": null,
  "state": "manual",
  "tagline": null,
  "nse_symbol": null
}
```

### 3.2 Field-by-field rationale

| Field | Value | Rationale |
|---|---|---|
| `id` / `slug` | `"onemi-technology-solutions"` | Same as the `ipo_id` used in `ipo-financials.json` + `ipo-documents.json` (Phase 5B.1 stripped the `curated_` prefix). Slug routing on `/ipo/:slug` matches this. |
| `name` | `"OnEMI Technology Solutions"` | From the staging snapshot's `company_name` field; also matches the BSE-hosted RHP title. |
| `segment` | `"mainboard"` | **Informed inference** from (a) revenue scale (1,559.90 cr 9M FY 26 — well above SME ceilings), (b) total assets 3,568.78 cr, (c) BSE Main-Board "IPO Open" URL path `/corporates/download/378749/IPO%20Open/...`, (d) RHP page count 464 (mainboard RHPs typically 400–700; SMEs typically 100–250). The `Segment` type is strict (`'mainboard' \| 'sme'` — no null option). |
| `status` | `"upcoming"` | **Conservative default**. We have no source-backed evidence of the exact `open_date` / `close_date` / `listing_date` in the staging snapshot. `'upcoming'` is the safest pre-listing status. Operator can override on a future Phase 5B.X-redux pass once dates land via NSE ingest or a separate curation. |
| `sector` | `null` | Sector classification (macro / sector / industry / basic_industry per NSE/SEBI taxonomy) is not in the staging snapshot. Phase 5B.X does NOT guess. |
| `open_date` / `close_date` / `listing_date` | `null` | Not in staging. Conservative null. |
| `price_band` / `lot_size` / `issue_size_cr` / `fresh_cr` / `ofs_cr` / `face_value` | `null` | None of these are in the staging snapshot's `line_items[]` (the staging captures restated financials, not the issue-terms cover page). A future Phase 5B.2 cover-extraction-for-OnEMI pass could populate them, but Phase 5B.X is master-row-only. |
| `listing_exchange` | `["BSE"]` | Strongly evidenced — the source PDF is hosted at `www.bseindia.com/corporates/download/378749/IPO%20Open/...`. NSE listing is not yet evidenced (could be added in a future pass if NSE-master ingest confirms it). |
| `reservation` | `null` | Not in staging. |
| `state` | `"manual"` | The row is operator-curated from a specific RHP filing — not from automated NSE/SEBI ingest. `state: 'manual'` matches the existing pattern for hand-authored rows (e.g. greendale-cement, quasar-robotics). `state: 'live'` is reserved for ingest-fed rows; we don't have the ingest-pipeline connection for OnEMI yet. **Phase 5C closure stands**: no new `DataState` values; no `SourceTag` extension. |
| `tagline` | `null` | Operator can add a short tagline later if desired. |
| `nse_symbol` | `null` | OnEMI hasn't been ingested via NSE master yet. |

The Phase 5B.X plan deliberately leaves 14 of 19 fields `null`. This is the audit-honest minimum that satisfies the `Ipo` type constraints without faking unverified data.

### 3.3 Field classification (binding — every field in the OnEMI master row carries exactly one tier)

To prevent future operators from mistaking the sparse master row for fully-verified issue-term data, every populated and null field is classified into one of four tiers:

- **`verified`** — directly read from a source-backed artifact already in the repo (staging JSON, Phase 5B.1-promoted snapshot row, or a real RHP URL committed to `ipo-documents.json`). The value is what the source says.
- **`inferred`** — not in any source artifact verbatim, but reasoned from multiple independent evidence signals (URL paths, financial scale, page counts, etc.). Documented in §3.2 rationale; could change if the inference turns out wrong.
- **`conservative default`** — no source-backed evidence either way; a deliberately safe value chosen to minimise downstream UI risk (e.g. `'upcoming'` rather than guessing dates). Operator can override on a future pass once evidence lands.
- **`unknown/null`** — no source-backed evidence available; explicit `null` to honour the `Ipo` type's nullability without faking data.

| Tier | Fields | Why this tier |
|---|---|---|
| **verified** | `id`, `slug`, `name`, RHP/document linkage (evidence basis — not a direct `Ipo` field; recorded via `state: 'manual'` + the §4 source basis trail back to `ipo-documents.json`) | Read directly from `phase-0/pdf-extracts/curated_onemi-technology-solutions/normalized-financials.json` (`ipo_id`, `company_name`); the BSE RHP URL is already in `ipo-documents.json` from Phase 5B.1 |
| **inferred** | `segment` (= `"mainboard"`), `listing_exchange` (= `["BSE"]`) | `segment` inferred from financial scale + URL path + page count (§3.2 rationale); `listing_exchange` inferred from the BSE host of the source PDF URL (only BSE evidenced; NSE listing not yet confirmed) |
| **conservative default** | `status` (= `"upcoming"`), `state` (= `"manual"`) | `status` = no source-backed dates → safest pre-listing value; `state` = `"manual"` matches the convention for hand-curated rows (no automated ingest pipeline for OnEMI yet) |
| **unknown/null** | `sector`, `open_date`, `close_date`, `listing_date`, `price_band`, `lot_size`, `issue_size_cr`, `fresh_cr`, `ofs_cr`, `face_value`, `reservation`, `tagline`, `nse_symbol` | Not in any source artifact; explicit `null` — Phase 5B.X does NOT guess |

**Tier count summary**: 4 verified (id/slug/name + RHP-linkage evidence), 2 inferred (segment, listing_exchange), 2 conservative defaults (status, state), 13 unknown/null. Total: 19 master-row fields tracked (`id` and `slug` carry the same value but are separately enumerated in the schema).

The `phase-5BX-status.md` written at Gate 2 close MUST reproduce this 4-tier classification verbatim so the audit trail is single-pane (§7 acceptance-gate item 16 binds this).

---

## 4. Source basis

Every populated field traces to source-backed evidence already in the repo. **No web fetch, no Chittorgarh fields, no broker/aggregator fields, no LLM inference, no new PDF download.**

| Populated field | Source |
|---|---|
| `id` / `slug` | `phase-0/pdf-extracts/curated_onemi-technology-solutions/normalized-financials.json` (`ipo_id`, prefix-stripped by the Phase 5B.1 promoter) |
| `name` | Same staging file, `company_name` field |
| `segment: "mainboard"` | Informed inference from financial scale + BSE Main-Board URL path. **Documented in Gate 2 status doc.** |
| `status: "upcoming"` | Conservative default. No source-backed `listing_date` available — `'upcoming'` is the audit-honest pre-listing label. |
| `listing_exchange: ["BSE"]` | `src/data/snapshots/ipo-documents.json` → `by_ipo['onemi-technology-solutions'].docs[0].url` host = `www.bseindia.com` |
| `state: "manual"` | Matches the convention for hand-curated rows; no automated ingest evidence yet. |

---

## 5. UI compatibility check

Phase 5B.X's central risk is **adding a sparse master row may crash a UI component that assumes a populated field is non-null**. The Gate 2 implementation pass MUST perform a render-time verification on each of the 10 known consumers of `ipo-master.json`:

| Route / component | Path | Likely render path | Risk |
|---|---|---|---|
| `/open` | `src/pages/OpenUpcoming.tsx` | Filters `ipos[]` by `status in {'upcoming','open'}`. Renders card with `name`, `segment`, `open_date`, `close_date`, `price_band`, `issue_size_cr`. | **MEDIUM** — null `open_date` / `price_band` / `issue_size_cr` may cause `toLocaleDateString()` / `formatCurrency()` crashes if not null-guarded. |
| `/screener` | `src/pages/Screener.tsx` | Iterates all `ipos[]`, renders sortable table including price_band, issue_size_cr, revenue_3y_cagr_pct (cross-joined with `ipo-financials.json`). | **MEDIUM** — null cells. Sort comparators may need `null != null` handling. |
| `/ipo/:slug` | `src/pages/IpoDetail.tsx` | Hero block, timeline, financials chart, documents list. Joins `ipo-master` + `ipo-financials` + `ipo-documents` + `ipo-narrative`. | **HIGH** — most fields land here. Hero needs `name`, `segment`, `status` (all populated). Timeline needs `bid_open` / `bid_close` (null — but `ipo-master.json.timelines[]` doesn't have an OnEMI row at all). Financial chart needs `periods[]` (populated by Phase 5B.1). Documents list needs `docs[]` (populated). |
| `/pipeline` | `src/pages/Pipeline.tsx` | Likely shows status='upcoming' rows. | LOW — `status: 'upcoming'` matches; data is sparse but should render. |
| `/source-health` | `src/pages/SourceHealth.tsx` | Source-mix aggregate. | LOW — OnEMI not in `ipo-source-audit.json`; should treat as "no audit row" gracefully. |
| `/subscription-heatmap` | `src/pages/SubscriptionHeatmap.tsx` | Joins `ipo-master` + `ipo-subscriptions`. | LOW — no subscriptions for OnEMI; component must already handle the case of an IPO without subscription rows. |
| `/listing-soon` | `src/pages/ListingSoon.tsx` | Filters by `listing_date` proximity. OnEMI's `listing_date: null` → does NOT appear here. | LOW — null `listing_date` excludes OnEMI from this route. |
| `/recently-listed` | `src/pages/RecentlyListed.tsx` | Filters by `status='listed'`. OnEMI is `'upcoming'` → does NOT appear. | LOW — status filter excludes. |
| `/` (Market Pulse) | `src/pages/MarketPulse.tsx` | Aggregate KPIs across `ipos[]`. | LOW — adding one more `upcoming` row to a count shouldn't break anything. |
| Pipeline timeline table | `src/components/pipeline/PipelineTimelineTable.tsx` | Reads `ipo-master.json.timelines[]` (not `ipos[]`). | NONE — Phase 5B.X does NOT add a timeline row. |

### 5.1 Phase 5B.X verification plan (binding, in Gate 2's verification order)

1. Run `npm run build` — confirms typecheck holds; the dashboard bundles cleanly with the new sparse OnEMI row.
2. Run `npm run dev` locally (Vite); manually load each of the 10 routes; confirm no console errors or render crashes.
3. Specifically verify `/ipo/onemi-technology-solutions` renders the hero + financials chart (with OnEMI's 4 periods + 4 line items) + documents list (with the BSE RHP link).
4. If ANY route throws a render-time error: HALT the implementation pass, do NOT commit, report which component crashed on which null field, and ask the operator whether to (a) approve a targeted null-tolerance UI shim or (b) abandon Phase 5B.X.

---

## 6. Guardrails (binding)

### 6.1 Phase 5B.X must NOT

- Parse additional PDFs / open any new PDF in code paths (no `pdfplumber`, no `camelot`, no `npm run pdf`)
- Add new ingestion sources or modify `scripts/ingest/*`
- Add DB / Workers / cron / GMP production integration
- Use Chittorgarh fields anywhere (Phase 5C closure binds)
- Use any aggregator field (Phase 5C closure binds)
- Use LLM inference of any kind
- Make UI changes (`src/components/**`, `src/pages/**`, `src/lib/**`) — UNLESS a Gate 2 render-time crash forces a tiny null-tolerance shim AND the operator separately approves it
- Touch `.github/workflows/*`
- Commit PDF binaries or full-text dumps
- Extend `src/types/source.ts`
- Extend `src/types/ipo.ts` with new required fields (additive-optional is allowed if strictly necessary)
- Mutate the existing 10 IPO rows in `ipo-master.json`
- Mutate `ipo-financials.json`, `ipo-documents.json`, `ipo-narrative.json`, `ipo-source-audit.json` (Phase 5B.1 already wrote OnEMI to financials + documents; those rows are final for this phase)
- Mutate `timelines[]` or `source_meta` in `ipo-master.json`

### 6.2 Phase 5B.X IS allowed to

- Add ONE new entry to `ipo-master.json` `ipos[]` array (the OnEMI sparse row per §3)
- Update `ipo-master.json` top-level `generated_at_utc`
- Create a single-purpose linker script (e.g. `scripts/pdf/promote/onemi-master.ts`, mirroring the Phase 5B.1 promoter pattern) that performs read-existing → preflight (count + idempotency) → insert via string surgery → atomic write
- Write `phase-5BX-status.md` after the implementation run

---

## 7. Acceptance gate

Phase 5B.X implementation can be accepted only if ALL hold post-implementation:

1. OnEMI appears in `ipo-master.json` `ipos[]` with `id: 'onemi-technology-solutions'`, `slug: 'onemi-technology-solutions'`, `segment: 'mainboard'`, `status: 'upcoming'`, `state: 'manual'`, `listing_exchange: ['BSE']`.
2. All 14 unverified fields in the OnEMI row are explicit `null` (per §3), not omitted, not undefined, not faked.
3. Existing 10 IPO rows in `ipo-master.json` `ipos[]` byte-identical pre/post (verified via `json.dumps(sort_keys=True)` equality).
4. `timelines[]` and `source_meta` byte-identical pre/post.
5. `ipo-financials.json` unchanged (`git diff` empty for this file).
6. `ipo-documents.json` unchanged.
7. `ipo-narrative.json` unchanged.
8. `ipo-source-audit.json` unchanged.
9. Provenance traceable: §3 / §4 documented in `phase-5BX-status.md` for every populated field.
10. `npm run typecheck` green — the OnEMI row validates against the `Ipo` interface.
11. `npm run build` green — the dashboard bundles cleanly with the new row present.
12. Manual UI verification passes (operator visits `/ipo/onemi-technology-solutions` and the 10 routes; no render-time crashes on the new row).
13. If a render crash occurred AND a null-tolerance UI shim was applied: the shim was separately approved BEFORE landing, scoped to the minimum, and documented in the status doc.
14. No PDF binaries or full-text dumps staged. No workflows touched. `src/types/source.ts` untouched. `scripts/ingest/*` untouched.
15. `phase-5BX-status.md` records the run with: the OnEMI row written, list of unknown fields with explicit reasons, manual UI verification outcome, and the operator-facing follow-up questions (Phase 5B.2 cover-extraction for issue terms; future label-dictionary tuning for missing financial line items).
16. `phase-5BX-status.md` explicitly lists every field in the OnEMI master row under one of the four §3.3 tiers — `verified`, `inferred`, `conservative default`, `unknown/null` — reproducing the §3.3 table verbatim. This is binding so future operators do not mistake the sparse master row for fully verified issue-term data: any later reader can see, in one glance, exactly which 4 fields are source-backed, which 2 are reasoned inferences, which 2 are deliberate defaults, and which 13 are explicit unknowns.

---

## 8. Implementation prompt for the Phase 5B.X Gate 2 pass

> Use this prompt verbatim when launching the Phase 5B.X Gate 2 execution pass. Implementation must not start until the operator explicitly approves this prompt as a separate, post-Gate-1 decision.

```
Phase 5B.X — OnEMI master/detail linkage (one row to ipo-master.json).

In-scope file changes:
  - scripts/pdf/promote/onemi-master.ts (NEW; single-purpose OnEMI master
    linker — mirrors the Phase 5B.1 promoter pattern at
    scripts/pdf/promote/onemi.ts: read-existing → preflight (count +
    idempotency) → string-surgery splice into ipos[] → atomic write)
  - src/data/snapshots/ipo-master.json (ADD one entry to ipos[];
    update generated_at_utc; preserve all 10 existing ipos[] rows
    byte-identically; preserve timelines[] + source_meta byte-identically)
  - phase-5BX-status.md (NEW; written after the linker run with
    verification evidence + manual UI verification outcome + operator
    follow-up questions)

Out of scope (HARD — same guardrails as Phase 5B.1):
  - src/data/snapshots/ipo-financials.json (do NOT mutate)
  - src/data/snapshots/ipo-documents.json (do NOT mutate)
  - src/data/snapshots/ipo-narrative.json (do NOT mutate)
  - src/data/snapshots/ipo-source-audit.json (do NOT mutate)
  - The 10 existing ipos[] entries in ipo-master.json
  - timelines[] / source_meta in ipo-master.json
  - src/types/source.ts (Phase 5C closure)
  - src/types/ipo.ts (no new required fields)
  - scripts/ingest/* (do NOT touch)
  - All UI files (src/components, src/pages, src/lib) UNLESS a Gate 2
    render-time crash forces a tiny null-tolerance shim AND the operator
    separately approves it BEFORE landing
  - .github/workflows/* (do NOT touch)
  - No new dependencies
  - No DB, Workers, cron, GMP, LLM, Chittorgarh fields, aggregator fields
  - No PDF binaries or full-text dumps
  - No new PDF parsing / extraction
  - Multi-IPO scaling (OnEMI only)

Linker behaviour (scripts/pdf/promote/onemi-master.ts):
  - Read src/data/snapshots/ipo-master.json via readJsonOrNull.
  - Preflight: verify ipos[] has exactly 10 existing entries; verify
    'onemi-technology-solutions' is NOT among them (idempotency).
    Any failure → HALT, exit 1, write nothing.
  - Build the OnEMI master row per §3 mapping.
  - Splice the new row into ipos[] via string-surgery (preserves
    byte-identical existing entries — the previous-last entry gets a
    trailing comma; the new entry is inserted before the ipos[] closing
    bracket; generated_at_utc updated; timelines[] and source_meta
    blocks left untouched). No JSON.stringify reflow.
  - Atomic write via .tmp + rename.
  - Print the OnEMI row written (id, slug, segment, status, state,
    listing_exchange) and the 14 explicit-null fields with reasons.
  - Exit 0 on success.

Hard guardrails:
  1. The linker is OnEMI-only. It must not iterate over other ipo_ids.
  2. The 10 existing ipos[] entries stay byte-identical (excepting the
     top-level generated_at_utc field).
  3. timelines[] and source_meta blocks stay byte-identical.
  4. typecheck + build pass after the write.
  5. Stay on main; no feature branches.
  6. Unverified fields are explicit `null`. segment is 'mainboard'
     (informed inference per §3.2). status is 'upcoming' (conservative
     default). state is 'manual'.

Verification order (binding — linker runs BEFORE typecheck/build,
                    UI verification runs LAST):
  (a) Preflight: confirm src/data/snapshots/ipo-master.json exists, has
      exactly 10 ipos[] entries, none with id 'onemi-technology-solutions'.
      Any failure → linker HALTS without writing.
  (b) Run: npx tsx scripts/pdf/promote/onemi-master.ts (atomic safe-merge
      writes to ipo-master.json).
  (c) Diff src/data/snapshots/ipo-master.json: only the OnEMI entry added
      to ipos[]; generated_at_utc updated; 10 existing ipos[] entries
      byte-identical; timelines[] + source_meta byte-identical (verified
      via json.dumps(sort_keys=True) equality).
  (d) Confirm src/data/snapshots/ipo-financials.json untouched.
      Confirm src/data/snapshots/ipo-documents.json untouched.
      Confirm src/data/snapshots/ipo-narrative.json untouched.
      Confirm src/data/snapshots/ipo-source-audit.json untouched.
      Confirm src/types/source.ts untouched.
      Confirm src/types/ipo.ts untouched (no new required fields).
      Confirm scripts/ingest/* untouched.
      Confirm src/components, src/pages, src/lib untouched.
      Confirm .github/workflows/* untouched.
      Confirm no PDF binaries or full-text dumps staged.
  (e) npm run typecheck (validates the new OnEMI row against the Ipo
      interface; specifically the strict 'mainboard' | 'sme' Segment
      and the 5-value IpoStatus enum).
  (f) npm run build (validates the dashboard bundles cleanly).
  (g) Manual UI verification (operator runs npm run dev and visits each
      of the 10 routes consuming ipo-master.json — full list in §5
      table). The route /ipo/onemi-technology-solutions is the critical
      one. If ANY route throws a render-time error: HALT, do NOT commit,
      report which component crashed on which null field, and ask the
      operator whether to (a) approve a targeted null-tolerance UI shim
      or (b) abandon Phase 5B.X.
  (h) Write phase-5BX-status.md with:
      - the OnEMI row written (all 19 fields)
      - the 14 explicit-null fields with reasons per §3
      - the §3.3 four-tier field classification reproduced verbatim
        (verified / inferred / conservative default / unknown/null) — per
        §7 acceptance-gate item 16; this is the audit guarantee that
        future operators cannot misread the sparse master row as
        fully-verified issue-term data
      - confirmation of all §7 acceptance-gate items (including #16)
      - manual UI verification outcome (per-route render check)
      - operator follow-up questions: (i) approve Phase 5B.2
        cover-extraction-for-OnEMI to populate issue-terms fields
        (open_date / close_date / listing_date / price_band / lot_size /
        issue_size_cr / face_value); (ii) approve future selector-tuning
        pass to populate ebitda / debt / eps from re-extracted RHP
        tables; (iii) approve sector classification pass.
  (i) Commit + push to main.

After push:
  - STOP. Wait for operator review.
  - Do NOT start any follow-up pass (5B.2 cover-extraction; selector
    tuning; sector classification) without explicit further approval.
```

---

*End of Gate 1 planning document. Gate 2 implementation requires separate explicit operator approval of §8 above.*
